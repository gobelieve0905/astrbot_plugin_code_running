# 更新记录

## 0.1.1 — 2026-09-14

- 修复管理页面在 AstrBot 桥接 SDK 加载前初始化，导致任务列表读取失败、刷新无效的问题。
- 页面完成解析后再获取桥接 SDK，并等待桥接就绪；初始化失败时提示重新打开页面。
- 新增页面脚本先于 SDK 加载、SDK 延迟就绪及任务刷新/停止的回归测试。

## Unreleased

### 2026-09-14

- 通用批量任务指引改由请求钩子按已授权工具自动提供，无需修改人格；插件或工具关闭、当前请求未授权时不注入，不扩展工具权限。
- 后台新增可按章节查看的批量任务指南，支持加载失败重试；明确独立执行服务安装要求及原生技能列表的区别。

- 新增固定只读 code_guide 工具和通用批量任务 Skill，按需提供预算规划、分页、检查点续接、跨来源聚合与完整性核对；无需开放原生终端，权限与代理仍由 API 插件执行。
- 以合成分页和聚合数据验证指南示例，补充真实 AstrBot 工具注册、只读章节及关闭状态回归检查。

- 调用上限改为管理员配置（默认 200，最高 10000），任务展示剩余额度与成功/失败计数；额度、超时及执行错误分开返回。
- 每次 API 响应自动落盘为受会话用户隔离的有界结果日志；新增确认写入的 JSON 检查点，可通过 previous_files 在新授权任务中续接，失败或停止保留已保存数据，不自动重放写请求。
- 保留全量调用审计（账户、查询指纹、游标指纹、行数、分页状态、耗时），模型状态仅返回近期记录；生成完整审计文件，不把执行成功等同于业务范围查全。
- 新增独立 Python 隔离执行、会话用户任务隔离、长任务状态、停止、附件输入、结果文件与后台审计。
- 新增受控 API 客户端；通过通用 Unix JSON RPC 对接 API 插件，不导入其代码或读取其配置。
- 新增固定策略执行服务：无网络、非 root、只读根文件系统、资源上限和断开连接自动回收。
- 新增离线 AstrBot 集成、权限撤销、任务隔离及容器安全检查。

- 2026-09-14：复核共享主机资源预算，将单任务内存上限设为 192 MiB，使现有容器与执行任务的内存上限合计为 2032 MiB，保留原 2 GiB 业务总组上限。
